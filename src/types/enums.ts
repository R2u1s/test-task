export enum FilterStates {
  Default = "all",
  Art = "art",
  Biography = "biography",
  Computers = "computers",
  History = "history",
  Medical = "medical",
  Poetry = "poetry"
}

export enum SortStates {
  Default = "relevance",
  Newest = "newest"
}